# ============================================================================
# Silk Road Caravans — Per-Game Override
# ============================================================================
#
# Silkroad chạy localhost:3000 không contextPath → match core defaults.
# Không cần khai gì ở đây. File giữ làm placeholder + audit trail.
#
# Nếu staging/prod đổi port, override qua CLI:
#   -Dport=8080 -Dhost=staging.example.com
# ============================================================================
